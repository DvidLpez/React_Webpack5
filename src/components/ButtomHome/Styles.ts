import styled from "styled-components";
import { Wrapper } from "../../components/StyledComponents/styledComponents";

export const WrapperLink = styled(Wrapper)`
  padding-top: ${(props) => props.theme.spaces.s20};
  text-align: center;
`;


